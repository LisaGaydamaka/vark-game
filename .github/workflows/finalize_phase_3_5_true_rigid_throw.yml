name: Finalize Phase 3.5 true rigid throw

on:
  push:
    branches:
      - test
    paths:
      - ".github/workflows/finalize_phase_3_5_true_rigid_throw.yml"

permissions:
  contents: write

jobs:
  finalize-phase-3-5-true-rigid-throw:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout authoritative test head
        uses: actions/checkout@v5
        with:
          ref: test
          fetch-depth: 0

      - name: Verify authoritative trigger head
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          git fetch origin test
          test "$(git rev-parse origin/test)" = "$GITHUB_SHA"

      - name: Apply collision-channel and solver launch correction
        shell: bash
        run: |
          set -euo pipefail
          python tools/phase35_true_rigid_throw_fix_tmp.py
          python tools/phase35_solver_launch_fix_tmp.py
          python -m py_compile \
            tools/phase35_true_rigid_throw_tmp.py \
            tools/phase35_solver_launch_tmp.py
          python tools/phase35_true_rigid_throw_tmp.py
          python tools/phase35_solver_launch_tmp.py
          rm -f \
            .github/workflows/finalize_phase_3_5_true_rigid_throw.yml \
            tools/phase35_true_rigid_throw_tmp.py \
            tools/phase35_true_rigid_throw_fix_tmp.py \
            tools/phase35_solver_launch_tmp.py \
            tools/phase35_solver_launch_fix_tmp.py
          find tools -type d -name __pycache__ -prune -exec rm -rf {} +
          git diff --check

      - name: Verify bounded production tree
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import subprocess
          expected = {
              ".github/workflows/finalize_phase_3_5_true_rigid_throw.yml",
              "tools/phase35_true_rigid_throw_tmp.py",
              "tools/phase35_true_rigid_throw_fix_tmp.py",
              "tools/phase35_solver_launch_tmp.py",
              "tools/phase35_solver_launch_fix_tmp.py",
              "project.godot",
              "player/Player.tscn",
              "gameplay/props/OrdinaryProp.tscn",
              "gameplay/props/ordinary_prop.gd",
              "gameplay/doors/OrdinaryDoor.tscn",
              "tests/props/phase_3_5_transition_regressions.gd",
              "docs/DEVELOPMENT_PLAN.md",
              "docs/GAME_VISION.md",
              "docs/TESTING.md",
          }
          raw = subprocess.check_output(["git", "status", "--porcelain=v1", "-uall"], text=True)
          changed = set()
          for line in raw.splitlines():
              if not line:
                  continue
              path = line[3:]
              if " -> " in path:
                  path = path.split(" -> ", 1)[1]
              changed.add(path)
          if changed != expected:
              raise SystemExit(
                  "bounded tree mismatch\nexpected=" + repr(sorted(expected)) +
                  "\nactual=" + repr(sorted(changed))
              )
          print("BOUNDED PRODUCTION TREE PASSED")
          PY

      - name: Setup Godot 4.7.2
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.7.2
          use-dotnet: false
          include-templates: false

      - name: Import project
        run: timeout 120s godot --headless --path . --import

      - name: Run focused Props suite
        run: timeout 150s godot --headless --path . --script res://tests/props/run_prop_tests.gd

      - name: Run authoritative full barrier
        run: timeout 240s godot --headless --path . --script res://tests/run_all_tests.gd

      - name: Commit and push verified production tree
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin test
          test "$(git rev-parse origin/test)" = "$GITHUB_SHA"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            .github/workflows/finalize_phase_3_5_true_rigid_throw.yml \
            tools/phase35_true_rigid_throw_tmp.py \
            tools/phase35_true_rigid_throw_fix_tmp.py \
            tools/phase35_solver_launch_tmp.py \
            tools/phase35_solver_launch_fix_tmp.py \
            project.godot \
            player/Player.tscn \
            gameplay/props/OrdinaryProp.tscn \
            gameplay/props/ordinary_prop.gd \
            gameplay/doors/OrdinaryDoor.tscn \
            tests/props/phase_3_5_transition_regressions.gd \
            docs/DEVELOPMENT_PLAN.md \
            docs/GAME_VISION.md \
            docs/TESTING.md
          git diff --cached --check
          git commit -m "Restore true rigid prop throwing"
          git push origin HEAD:test
