# Tool reference snapshots

- `trenchbroom-2026.2-reference-manual.html` — user-supplied snapshot of the **TrenchBroom 2026.2 Reference Manual** used for version-specific mapper/FGD behavior during Phase 3.6 diagnosis. The HTML text is preserved in-repository for search and future reference; its external CSS/image assets are not duplicated here.

When implementation behavior depends on TrenchBroom 2026.2 semantics, prefer this pinned snapshot plus the project's deterministic integration tests over assumptions from newer/older editor versions.
